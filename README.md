# Here are your Instructions

## building:

cd frontend

yarn build:docs


## How to add a new language:

Copy /public/translations/en.yaml to a new file (e.g., es.yaml).

Translate all strings in the YAML file.

Save to /public/translations/.

Language is configured in /src/translations/config.js.
